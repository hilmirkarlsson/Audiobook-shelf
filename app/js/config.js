// Filled in during OAuth setup (docs/OAUTH_SETUP.md). Safe to commit:
// OAuth Client IDs are public by design (not a secret) for installed/web apps using PKCE.
export const CONFIG = {
  GOOGLE_CLIENT_ID: 'REPLACE_ME.apps.googleusercontent.com',
  DRIVE_SCOPE: 'https://www.googleapis.com/auth/drive.file',
  DRIVE_FOLDER_NAME: 'Audiobook Shelf',
};
